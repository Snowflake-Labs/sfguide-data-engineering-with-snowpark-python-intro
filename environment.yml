name: snowflake-demo
channels:
  - https://repo.anaconda.com/pkgs/snowflake
  - nodefaults
dependencies:
  - python=3.10
  - pip
  - openssl=1.1.1
  - pip:
    # Snowflake
    - snowflake
    - snowflake-snowpark-python
